# Cold Mailing Tool — State & Roadmap

Last updated: 2026-06-09. This is the durable handoff: read it (plus `CLAUDE.md` + `AGENTS.md`) to resume after a context reset.

**▶ NEXT TASK: the BUILD IS CODE-COMPLETE — Phase 7 (and all phases 0-7) DONE.** **Post-phase polish `creation-ux` is DONE + verified 2026-06-10 (code/test/eslint green; `next build` deferred — dev live): creation moved into modals (lead = a 2-step wizard with a shared-layout morph) and every library page got a professional PageHeader.** Spec `docs/superpowers/specs/2026-06-10-creation-ux-design.md` (the animation spec authored with the /motion + /web-animation-design skills is BINDING: modal enter 200 ms ease-out-quart from scale .96 / exit 160 ms; step morph = Motion `layout` FLIP spring `visualDuration .25, bounce 0`; step content slides x±12 200 ms; `useReducedMotion` snaps everything), plan `docs/superpowers/plans/2026-06-10-creation-ux.md`, mock `.superpowers/brainstorm/2477-1781081234/content/creation-ux.html` (user picked step-indicator variant B: "Krok 1 z 2" + 2 px progress line). NEW primitives: `components/ui/modal.tsx` (native `<dialog>` per the ConfirmProvider idiom + Motion panel; stale exit-close guarded via an openRef so rapid close->reopen can't shut a fresh dialog — ref written in the effect, `react-hooks/refs`-clean) + `components/ui/page-header.tsx` (title/description/meta/action). **Lead wizard** `app/(app)/leady/lead-create-button.tsx`: 2 steps (Organizacja -> Kontakt) inside ONE `<form>` — both steps stay MOUNTED (inactive `hidden`; Tailwind v4 preflight `[hidden]{display:none !important}` beats the flex class — verified in-tree) so uncontrolled values survive 1->2->1 and FormData stays flat; the step slide is IMPERATIVE `animate()` from `motion/react` (no AnimatePresence/key inside the form = no remount), prev-step ref + reduced-motion skip; success side-effects (toast/reset/close) run INSIDE the `useActionState` wrapper (lint-safe, no setState-in-effect); per-step `reportValidity`. `createLead` gained ADDITIVE `honorific`/`phone`/`region` (columns existed; no migration); the form-contract set-diff over the whole change shows ONLY net-new names contactRole/honorific/phone/region (contactRole was already server-supported), nothing renamed/dropped. **Simple create modals** (same Modal + create-button-island pattern, forms gained optional `onSuccess` + toast + reset in the wrapper): kampanie/linie/placeholdery/suppression — inline forms REMOVED from above the lists. **PageHeaders everywhere**: leady ("N leadów · M w kampaniach" via 2 cheap counts in the existing Promise.all; Importuj link + "+ Dodaj leada"), kampanie ("N kampanii · M aktywnych"), linie/placeholdery/suppression (counts), szablony (anchor action `#edytor`; editor stays a page), badania ("N typów · M ostatnich badań zbiorczych"; "Nowy typ" link moved into the header), pipeline ("N leadów na tablicy"; heading stays "Pipeline"); EmptyState CTAs open the matching modal (variant="secondary"); row-hover polish (`transition-colors hover:border-border-strong`) on all six Card-row lists. `lead-form.tsx` deleted. Gates: `tsc` 0, `vitest` 258 (unchanged — no new pure logic), `eslint` clean on all touched code; reviews: T1+T2 PASS (1 Important fixed: the stale-close race; hygiene fixes applied; noted-not-fixed: `updateLead` still ignores phone/region — pre-existing edit-form scope), T3/T4 verbatim-plan + gates. NOT live-eyeballed by me (auth-gated; the user reviews the wizard morph + modals + headers on :3000 — reduced-motion path worth a check via OS setting). **Post-phase polish `dashboard-redesign` is DONE + verified 2026-06-10 (code/test/eslint green; `next build` still deferred, dev live): the pulpit was rebuilt from the 8-tile "AI slop" grid to a Vercel/Linear single-canvas (user-picked variant B via mock; full scope incl. data widgets).** Spec `docs/superpowers/specs/2026-06-10-dashboard-redesign-design.md`, plan `docs/superpowers/plans/2026-06-10-dashboard-redesign.md`, mock `.superpowers/brainstorm/1404-1781078715/content/dashboard-directions.html`. ONE `Card` canvas with hairline-divided sections: 4-number hero strip (divide-x, 26px tabular), a 30-day sends **sparkline** (server-rendered SVG from pure `buildSparklinePath`; gradient fill + end dot) with a sending-health trio (odbicia/rezygnacje/błędy as dots), thin 3px-bar funnel (WON green / LOST muted red) next to a **recent-replies feed** (initials avatar + org + campaign + `relativeTimePl`, links to the campaign), and a flat campaigns table with `CAMPAIGN_STATUS_*` Badges (fixes the dashboard's raw `{c.status}` enum leak — the spawned chip is narrowed to /skrzynki only). New org-scoped queries `getDailySends` (sentAt[] bucketed by pure `bucketDaily`, Europe/Warsaw days, zero-filled, fetch window = bucket window) + `getRecentReplies` (INBOUND REPLY/OPT_OUT_SUSPECT). Pure TDD +9 (`bucketDaily`, `relativeTimePl`, `buildSparklinePath`); `dashboard-parts.tsx` rebuilt (Kpi/FunnelBar deleted; `MetricStat` kept byte-identical for `/kampanie/[id]`); zero client JS, no migration, no behavior change outside `/`. Gates: `tsc` 0, `vitest` 258 (+9), eslint clean; review PASS (4 minor: 1 fixed [fetch-window off-by-one], 3 accepted [dead null-guard as reuse-safety, "wczoraj" 24-48h approximation, theoretical feed-key collision]). NOT live-eyeballed by me (auth-gated; user reviews on :3000). Remaining before launch, in order: (1) run `npx next build` once the dev server is OFF (deferred twice on 2026-06-09/10 — pure-presentation + role changes, tsc/vitest/eslint all green, break unlikely); (2) the user's live smoke of team-role-ui (invite an e-mail on /ustawienia -> Zespół, sign in with it on a second Google account, see MEMBER nav without Ustawienia/Skrzynki, promote to ADMIN, verify a gated action works WITHOUT re-login); (3) the funded `ANTHROPIC_API_KEY` live runs (research/scoring/content/draft) + verify `run-research`/`run-research-batch`/`run-draft` + the reply cron fire on Inngest Cloud. **Sub-project `team-role-ui` is DONE + verified 2026-06-10 (code/test/eslint green; `next build` deferred, dev live): the Role ladder MEMBER < ADMIN < OWNER is now ENFORCED and the team is managed in-app.** Built brainstorm (3 AskUserQuestion decisions + a visual mock [user picked variant B: table]) -> spec -> plan -> subagent-driven (8 tasks + adversarial security review). Spec `docs/superpowers/specs/2026-06-09-phase-7-team-role-ui-design.md`, plan `docs/superpowers/plans/2026-06-09-phase-7-team-role-ui.md`. Migration `phase7_team_role_ui` (additive: `Invitation` org-scoped {email lowercase, role, invitedById?, @@unique(org,email)}; SQL inspected additive-only, `--create-only` -> `migrate deploy` per memory). **Sign-in rework (`lib/auth-helpers.ts`):** allowed iff existing Membership OR pending Invitation OR `OWNER_EMAIL` (bootstrap); first sign-in consumes the invitation in a tx (Membership created with the invited role, row deleted); **`ALLOWED_EMAILS` is RETIRED** (deprecated in `.env.example`; removal of a member now sticks). Pure `features/team/{roles,guards,join}.ts` (TDD +14: `hasRole` ladder, `ownerCount`/`canChangeRole`/`canRemoveMember` last-OWNER invariant, `resolveJoin` precedence membership>invitation>owner-email>reject). **Enforcement is DB-FRESH:** `lib/org.ts` += `currentRole(ctx)` (membership re-read; throws when gone) + `requireRole(min)` — role changes/removal bite immediately in gated actions; the JWT/session role is UI politeness only. **Gating map (adversarial review PASS, 0 critical/important):** ADMIN+ = `activateCampaign`/`setSendingMailbox`, `disconnectEmailAccount`/`updateEmailAccountSettings`, `removeSuppression`, `setOrgName`/`setSenderSignature`/`setOrgInboundSettings`, the `/api/mailbox/google/connect` route, and fresh-check `redirect("/")` page guards on `/ustawienia` + `/skrzynki`; OWNER = the 4 team actions; MEMBER keeps everything else incl. `pauseCampaign` (safety), `addSuppression`, `markDoNotContact`. **Team module:** `features/team/queries.ts` `listTeam` + `features/team/actions.ts` (`inviteMember` Zod z.email lowercase + already-member/already-invited friendly errors, `cancelInvitation`, `changeMemberRole` -> result for toast, `removeMember`; all OWNER-gated, org-scoped writes, last-OWNER guarded server-side). **UI:** `/ustawienia` += section "Zespół" (OWNER-only; ADMIN sees the other sections): invite form (e-mail + rola + Zaproś) + ONE `Table` (members sorted OWNER->ADMIN->MEMBER with per-row insta-submit role `Select` + toast + ConfirmButton Usuń; the LAST owner shows Badge + "—"; pending invitations dimmed with Badge "Oczekuje" + Anuluj). Nav: `nav-items.tsx` `minRole` on Skrzynki + Ustawienia; sidebar filters fail-closed + shows the role via `ROLE_LABEL` (Właściciel/Administrator/Członek — last enum leak in the shell closed); campaign page hides mailbox+activate for MEMBER (pause visible). Gates: `tsc` 0, `vitest` 249 (+14), `eslint` clean on all touched code (the only failures = the pre-existing `any` mocks in 2 old inngest test files, documented project pattern). **Accepted trade-offs (documented in spec):** a REMOVED member's existing JWT keeps ungated MEMBER access <= token maxAge (30 d) — sign-in + gated actions blocked immediately; `OWNER_EMAIL` re-admits its own e-mail (recovery hatch). 2 pre-existing Minor crypto notes from the security review (OAuth state no-expiry/nonce; `TOKEN_ENCRYPTION_KEY` reused AES+HMAC) -> follow-up candidates, not blockers. NOT live-smoked (needs a second Google account — user-run). **Sub-project `page-level-visual-pass` is DONE + verified 2026-06-09 (code/test/eslint green; `next build` DEFERRED — the user's dev server was live on :3001): every remaining app surface brought up to the design-foundation bar — primitive adoption + full polonization of enum labels via a shared module + Card-row library lists, with ZERO behavior change.** Built split-spec/implement (spec prepared a prior session; THIS session ran writing-plans -> subagent-driven, 6 slices, two-stage review [spec compliance + code quality] per slice). Spec `docs/superpowers/specs/2026-06-09-phase-7-page-level-visual-pass-design.md`, plan `docs/superpowers/plans/2026-06-09-phase-7-page-level-visual-pass.md`. NEW pure `features/enums/labels.ts` (TDD totality test, +3): maps every leaking Prisma enum -> Polish label + `Badge` variant — `CAMPAIGN_STATUS_LABEL/_VARIANT`, `LEAD_STATUS_*` (CampaignLeadStatus), `CONDITION_LABEL`, `INBOUND_KIND_*`, `RESEARCH_KIND_*`, `RESEARCH_STATUS_LABEL/_VARIANT` (typed over the `ResearchRunStatus | ResearchBatchStatus` union so tsc enforces totality — fixes the old run-vs-batch label leak), `PLACEHOLDER_TYPE_LABEL`, `ROLE_LABEL`; `BadgeVariant` exported from `components/ui/badge.tsx`; `DealStage` reuses the existing `STAGE_LABEL`. The 6 slices (one commit each): (1) labels module; (2) **leady** — filters/forms -> Input/Select/Textarea/Field/Button, leads list -> `Table` primitive, `dealStage` -> Badge, copy (`Import`->`Importuj`, `(honorific)`->`Forma grzecznościowa`, `(customFields)`->`Pola własne`); (3) **kampanie** — list -> Card rows, status/condition/inbound -> shared Badges (deleted a local `BADGE` map + a hardcoded violet draft chip); (4) **badania** — list -> Card rows with all 4 kind badges incl. RESEARCH via the shared map (**removed the LAST amber/sky/violet dark-migration literals**), batch-view -> `Table` + shared `RESEARCH_STATUS_*`, local Target/FieldType Polish option maps, `webSearchEnabled` uncontrolled checkbox -> `Switch` + a `<input type=hidden>` mirror (submit contract `=== "true"` proven identical by an explicit truth table), `Switch` gained an optional `aria-label`; (5) **content** — szablony/placeholdery/linie -> Card rows, follow-up + `PlaceholderType` Badges, copy (`To follow-up`->`Wiadomość follow-up`, `opcje CHOICE`->`opcje listy wyboru`); (6) **suppression** (heading -> `Wykluczenia`, list -> Card rows) + **pipeline** (filter -> primitives; the draggable card KEEPS its native `<div>` + all HTML5 DnD handlers, just gains the `Card` class string; `sequenceStatus` -> Badge; `score`->`ocena`; board `+N więcej` -> ghost Button). Approved Card-row style = variant K (`<Card className="flex items-center justify-between gap-3 px-3.5 py-3">` + `min-w-0` title block + `text-[12px] text-fg-faint` meta). Gates: `tsc` 0, `vitest` 235 (+3 labels), `eslint` clean (`app/(app)` + `components/ui` + `features/enums`); **scope audit** = only `app/(app)/*` + `components/ui/badge.tsx` + `components/ui/switch.tsx` + `features/enums/*` (ZERO `actions`/`queries`/`lib`/`prisma`); a `name=` set-diff over the whole change proved NO form field was renamed/added/dropped. Two-stage review per slice -> all green after small fixes (Field in draft-panel, functional truncation on inline chips, DRY'd `RESEARCH_STATUS_VARIANT` into the shared module + test, suppression-form bare `<Input>`, className-first). Final holistic review = **PASS**; it surfaced 2 enum leaks on OUT-of-scope surfaces (`app/(app)/page.tsx` dashboard campaign status + `/skrzynki` mailbox status — the latter needs a new `EMAIL_ACCOUNT_STATUS` map) -> **deferred to a spawned task** (`task_4906ea35`). **`next build` NOT run** (dev live on :3001 -> `.next` conflict; tsc+eslint+vitest all green make a build break unlikely for a pure-presentation change — run it once dev is off). NOT live-eyeballed by me (auth-gated; the user reviews on :3001). After it: the low-priority `team-role-ui` (needs an Invitation-model decision; role is returned by `requireOrg` but never enforced) = the final Phase 7 piece. **Sub-project `shell-and-nav-redesign` is DONE + verified 2026-06-09 (code/test/build green): the flat top-bar nav is replaced by a left ICON SIDEBAR (variant C) that collapses to an icon-only rail, with grouped sections, a left-bar active state, and an account menu surfacing user/org/role.** Built brainstorm (two visual mocks — a 3-shell comparison [user picked C: icon sidebar, collapse-to-icons] + a refinement mock of the active-item finish [user picked B: left accent bar]) -> spec -> plan -> inline TDD + adversarial review. Spec `docs/superpowers/specs/2026-06-09-phase-7-shell-and-nav-redesign-design.md`, plan `docs/superpowers/plans/2026-06-09-phase-7-shell-and-nav-redesign.md`. Pure `features/nav/active.ts` `isNavActive` (TDD, +2; the `/` exact-match edge). ~13 stroke icons added to `components/ui/icons.tsx`. `app/(app)/nav-items.tsx` (grouped config: Sprzedaż/Treści/Wysyłka + standalone Ustawienia). Client `app/(app)/sidebar.tsx`: collapse flag via a module-level external store + `useSyncExternalStore` (getServerSnapshot=false -> no hydration mismatch, no setState-in-effect); responsive collapse in pure Tailwind (`w-14` + `lg:w-[212px]`; labels `hidden`/`hidden lg:inline`) so it is icon-only below `lg` and toggle-controlled at `lg+`; left-bar active state (`before:` accent bar flush to the rail edge + accent icon); `ThemeToggle` + account menu (native `<details>`, name/email/org/role + `signOutAction`, `aria-label`). `features/auth/actions.ts` `signOutAction` (moved the inline `signOut` closure here). `app/(app)/layout.tsx` refactored to `[Sidebar][main Container]` (fetches the org name; keeps providers + skip-link + auth redirect); `app/(app)/nav-link.tsx` deleted. No migration. Gates: `tsc` 0, `vitest` 232 (+2), `eslint` clean (incl. the `react-hooks/set-state-in-effect` rule — hence `useSyncExternalStore`), `next build` green. Review: all 6 checks passed (hydration, responsive logic, server-action wiring, layout-guard preservation); 1 important a11y fix (account-menu `aria-label`) + 1 taste fix (flush active bar) applied; native `<details>` not closing on outside-click accepted for an internal tool. NOT live-eyeballed by me (auth-gated; the user reviews on :3001 — high-fidelity mock matched). **Sub-project `dashboards` is DONE + verified 2026-06-09 (code/test/build green): a pulpit at `/` (org KPIs + sales funnel + campaigns table) + a per-campaign metrics strip, all from a new `features/metrics` (pure `compute.ts` + thin `queries.ts`); no open/click tracking, no migration.** Built brainstorm (two dark-themed mocks — the pulpit + a denominator comparison; user chose full scope + the "contacted" rate denominator) -> spec -> plan -> inline TDD + adversarial review. Spec `docs/superpowers/specs/2026-06-09-phase-7-dashboards-design.md`, plan `docs/superpowers/plans/2026-06-09-phase-7-dashboards.md`. Pure `features/metrics/compute.ts` (TDD, +8): `summarizeStatuses` (CampaignLead-status groupBy -> breakdown), `contacted` (= total − PENDING − SKIPPED − FAILED), `rate` (null on 0 denom), `computeRates`, `formatPct` (PL comma, "—" on null), `formatInt` (NBSP thousands via `String.fromCharCode(160)`), `buildCampaignRows`, `orderFunnel` (reuses `STAGE_ORDER`). `features/metrics/queries.ts` `getOrgMetrics`/`getCampaignMetrics` (org-scoped — CampaignLead via `campaign:{organizationId}`; Prisma `groupBy` `_count:{_all:true}`; `getCampaignMetrics` returns null for a foreign campaign). `/` MOVED from a redirect (`app/page.tsx` deleted) into the `(app)` auth shell as `app/(app)/page.tsx` + a "Pulpit" nav link; `dashboard-parts.tsx` (`Kpi`/`FunnelBar`/`MetricStat`, all server-rendered, no client JS); per-campaign metrics strip on `/kampanie/[id]`. Rates use the **contacted** denominator (variant A). No tracking model -> open/click omitted by design. Gates: `tsc` 0, `vitest` 230 (+8), `eslint` clean, `next build` green (`/` now a dynamic route). Review: all 6 correctness checks passed (org-scoping airtight incl. the indirect `campaign:{organizationId}` scope, `_count._all` mapping, divide-by-zero -> "—", route move clean, funnel-max guard, valid field reads); 1 minor fixed (NBSP separator). **Sub-project `deliverability-settings` is DONE + verified 2026-06-09 (code/test/build green): each connected mailbox now has an editable send-pacing config on `/skrzynki` (daily limit, send window, send days, random gap, timezone) saved via a new `updateEmailAccountSettings` action.** Built brainstorm (dark-themed visual mock; user chose **config-only** + curated timezone) -> spec -> plan -> inline TDD + adversarial review. Spec `docs/superpowers/specs/2026-06-09-phase-7-deliverability-settings-design.md`, plan `docs/superpowers/plans/2026-06-09-phase-7-deliverability-settings.md`. Pure `features/email-accounts/settings.ts` (TDD, +10): `parseTime`/`formatTime` (HH:MM<->min), `maskToDays`/`daysToMask` (bit0=Mon..bit6=Sun), `isValidTimezone` (Intl), `validateEmailAccountSettings` (bounds: limit 1-1000, start<end in 0..1440, 0<=minGap<=maxGap<=86400, >=1 day, valid tz) -> the `EmailAccount` update payload. New `updateEmailAccountSettings` action (org-scoped `updateMany`, `revalidatePath`) + expanded `listEmailAccounts` select. `/skrzynki` rebuilt as per-mailbox cards (status + reply badges + `ConfirmButton` Odłącz) + `mailbox-settings-form.tsx` (uncontrolled defaults, native `<input type=time>`, day checkbox chips via the `peer` pattern, curated tz `<Select>` that preserves a saved custom zone). No migration (all 7 fields already existed on `EmailAccount`; they map 1:1 to the engine's `PacingAccount`). **Scoped OUT by user choice (config-only):** the mailbox-health section + the SPF/DKIM/DMARC guidance panel (visual mock kept at `docs/superpowers/design/deliverability-mock/index.html`) — both a clean later add. Gates: `tsc` 0, `vitest` 222 (+10), `eslint` clean (`app/(app)` + `features` + `components`), `next build` green. Review: all 6 correctness checks passed (field-name contract, checkbox-deselect, cleared-input, tz fallback, org-scoping, no regression); 1 minor (step=1 on number inputs) fixed; window end=1440 confirmed safe (engine uses half-open `< end`, minutes 0..1439). **Sub-project `import-customfields-mapping` is DONE + verified 2026-06-09 (code/test/build green): the CSV importer now routes ANY column to `Lead.customFields` via a column->target wizard (it no longer drops unknown columns), and the lead-detail customFields editor gained a remove-row button.** Built brainstorm (with a dark-themed visual mock comparing two UX variants — the user picked column->target) -> spec -> plan -> inline TDD + adversarial code review. Spec `docs/superpowers/specs/2026-06-09-phase-7-import-customfields-mapping-design.md`, plan `docs/superpowers/plans/2026-06-09-phase-7-import-customfields-mapping.md`. Pure `features/leads/import.ts`: new `buildImportSpec` (column-target[] -> {known-field mapping, custom columns}, last-wins) + `defaultCustomKey` + `customFields` on `LeadImportInput` + `mapRowsToLeads` builds the custom bag (skips empty cells, null when none); `importLeads` parses per-column `target_<i>`/`customkey_<i>` form fields, and `createMany` coerces `customFields ?? undefined` for the Prisma `Json?` nullable input (never passes a literal null). Wizard rewritten column-centric (one target dropdown per column; resets the mapping on column-count change — a review fix). No migration (`Lead.customFields Json?` already existed). Gates: `tsc` 0, `vitest` 212 (+5 import cases), `eslint` clean, `next build` green. Adversarial review: 1 important finding (stale index-keyed mapping on re-paste) fixed; 2 minor accepted (defensible for a trusted-operator tool). **Sub-project `destructive-action-safety` is DONE + verified 2026-06-09 (code/test/build green): a new `ConfirmButton` primitive (`components/ui/confirm-button.tsx`) guards all 13 destructive actions with a confirm dialog + pending spinner + danger styling + success toast.** Built brainstorm -> spec -> plan -> subagent-driven fan-out (Task 1 primitive inline, then 3 adoption groups each independently audited — 0 corrections). Spec `docs/superpowers/specs/2026-06-09-phase-7-destructive-action-safety-design.md`, plan `docs/superpowers/plans/2026-06-09-phase-7-destructive-action-safety.md`. `ConfirmButton` wraps a bound server action: awaits the existing `useConfirm()` dialog, runs the action in a `useTransition` (pending), then fires `useToast`. Adopted at 12 sites (`<form action={deleteX.bind}>` -> `<ConfirmButton>`: research-type / placeholder / offering-line / template / campaign / lead / lead-activity / sequence-step / suppression deletes + disconnect-mailbox + pause-campaign + mark-do-not-contact) + the `cancelBatch` client handler gated in-place (returns a result, so no primitive). Excluded by design: the research-type-form unsaved remove-field (local state) + sign-out. Gates: `tsc` 0, `vitest` 207 (unchanged), `eslint` clean (`app/(app)` + `components`), `next build` green; a guard grep confirms zero destructive `<form>` remain. NOT live-eyeballed (UI glue; statically verified by independent per-group audits). **Sub-project `dark-ready token migration` is DONE + verified 2026-06-09 (code/test/build green): the runtime default theme is now DARK and all 33 pages are token-based.** Built subagent-driven via a fan-out workflow (9 tasks: 7 slice color->token migrations + default flip + gates; one shared canonical map + 8 resolved ambiguities; grounded by the `dark-ready-color-inventory` inventory). Spec `docs/superpowers/specs/2026-06-09-phase-7-dark-ready-migration-design.md`, plan `docs/superpowers/plans/2026-06-09-phase-7-dark-ready-migration.md`. Swapped literal `zinc-*`/`white`/status colors -> design-foundation token utilities (`text-fg`/`text-fg-muted`/`text-fg-faint`, `bg-surface-1/2`, `bg-primary`+`text-primary-fg`, `border-border`/`border-border-strong`, `text-accent`/`text-danger`/`text-success`/`text-warning`, `bg-accent-quiet`, `divide-border`) across 33 files; the 8 resolved exceptions held (solid amber CTAs -> `bg-primary text-primary-fg`, suppression "Usuń" -> `text-danger`, `leady` table-header -> `border-border-strong`, research-KIND category badges left as light-tint chips = the ONLY literals remaining, by design — proper dark-native category tokens stay a `page-level-visual-pass` enhancement). Then flipped the runtime default light->dark (`features/theme/resolve.ts` + the mirrored `app/layout.tsx` no-flash script + `<html data-theme>`; `resolve.test.ts` updated). Gates: `tsc` 0, `vitest` 207, `eslint` clean (`app/(app)`), `next build` green; a guard grep confirms only the 4 category-badge lines keep literals. NOT live-eyeballed in a running dark UI (pure class-name swap; statically verified by an independent per-slice audit + the guard grep). After it: `destructive-action-safety` (the `ConfirmDialog`/`useConfirm` primitives now exist), then `import-customfields-mapping`, `deliverability-settings`, `dashboards`, and the high-taste `shell-and-nav-redesign` + `page-level-visual-pass` (which now owns only deeper polish: primitive adoption, spacing/copy, dedicated category-badge tokens). Phase 7 was inventoried + decomposed (workflow, 2026-06-09) into 9 independently-shippable sub-projects; recommended order: `design-foundation`[DONE] -> `sender-signature`[DONE] -> `dark-ready token migration`[DONE] -> `destructive-action-safety`[DONE] -> `import-customfields-mapping`[DONE] -> `deliverability-settings`[DONE] -> `dashboards`[DONE] -> `shell-and-nav-redesign`[DONE] -> `page-level-visual-pass` -> `team-role-ui` (full decomposition + per-slice inventory in the Phase 7 line below; #7/#8 are high-taste and wait for user design direction). **Phase 7 sub-project `design-foundation` is DONE + verified (2026-06-09): code/test/build green; built subagent-driven (10 tasks, spec+quality review each).** Spec `docs/superpowers/specs/2026-06-09-phase-7-design-foundation-design.md`, plan `docs/superpowers/plans/2026-06-09-phase-7-design-foundation.md`, visual sketch `docs/superpowers/design/design-foundation-sketch.html`. Vercel/Linear refined-minimal: Tailwind v4 `@theme inline` tokens (dark + light; **runtime default LIGHT during migration** so un-converted pages stay readable — flips to dark after `page-level-visual-pass`; `[data-theme]` + no-flash inline script + `ThemeToggle` + localStorage), Inter + Geist Mono (`next/font` + the `geist` pkg for mono), muted indigo accent (`#7c86dd`/`#4f56c6`), sharp 4/6/8px radii, flat (no grain). `components/ui/*` primitives (Button/Badge/Input/Textarea/Select/Field/Note/Card/Table/Switch/Spinner/EmptyState/Icons/Container/ThemeToggle) on `lib/cn.ts`; scaffolding (`ToastProvider`+`useToast`, `ConfirmProvider`+`useConfirm` on native `<dialog>`, route-level `loading/error/not-found`, skip link, reduced-motion reset). `/ustawienia` rebuilt as the reference surface (settings shell + **fluid body** via `motion` AnimatePresence; sections Ogólne/Podpis nadawcy/Wykrywanie rezygnacji, forms migrated to primitives + toast). New deps: `motion`, `geist`. Gates: `tsc` 0, `vitest` 207 (+9 pure: theme-resolve 2, button-variant 3, toast-reducer 2, confirm-state 2), `next build` green, eslint-clean (new code). NOT live-run (pure UI). Caveat documented: only `/ustawienia` + the nav are theme-correct; other pages stay on literal `zinc-*` until `page-level-visual-pass`. **`sender-signature` (sub-project 2) is DONE + verified (2026-06-09): code/test/build green.** Spec `docs/superpowers/specs/2026-06-08-phase-7-sender-signature-design.md`, plan `docs/superpowers/plans/2026-06-08-phase-7-sender-signature.md`. Fixes the verified preview-vs-send `{{podpisNadawcy}}` divergence (preview used the logged-in user name, send used the mailbox name): new per-org `Organization.senderSignature` (migration `phase7_sender_signature`, additive) is the single source via pure `features/templates/sender.ts` `resolveSenderName({signature, fallback})` used by BOTH the send worker `lib/inngest/sending.ts` and the editor preview `app/(app)/szablony/page.tsx`; empty signature -> mailbox `displayName` fallback, **flagged** in `/ustawienia` (new `setSenderSignature` action + `SenderSignatureForm`) and the editor preview note. `render.ts` unchanged. Gates: `tsc` 0, `vitest` 198 (+8: sender 6, render 2), `next build` green, eslint-clean (prod). Still open before launch (blocked on a funded `ANTHROPIC_API_KEY` — see memory): a first live research/scoring/content/draft run, and verifying `run-research`, `run-research-batch`, `run-draft`, and the reply cron fire on Inngest Cloud. Optional later: batch draft generation (fan-out like 2c). **Phase 2d-4 (use the per-lead AI draft when sending) is DONE + verified (2026-06-08): code/test/build green; NOT live-run.** Plan `docs/superpowers/plans/2026-06-08-phase-2d-4-send-draft.md`, spec `docs/superpowers/specs/2026-06-08-phase-2d-4-send-draft-design.md`. First change to the verified sending worker `lib/inngest/sending.ts`: per-step `SequenceStep.useLeadDraft` flag (migration `phase2d4_send_draft`, additive; `templateId` stays required, unused for draft steps); when set, the SEND branch sends the lead's `LeadDraft` subject/body **verbatim** (no token render); if the draft isn't DONE it **defers via `step.waitForEvent("research/draft.ready", {timeout:"7d", if:"async.data.leadId == event.data.leadId"})`** then re-checks (send if now ready, else advance without sending — 7d timeout). Pure `features/sequences/draft-send.ts` `resolveDraftDecision` (template/draft/wait). `research/draft.ready {leadId, organizationId}` emitted by `runDraftFn` (on DONE) + `saveDraft`; `campaign/lead.start` gained `leadId` for the match. Sequence-builder checkbox ("Wyślij draft AI") + step marker. **Invariant held: the `useLeadDraft===false` path is regression-guarded byte-identical** (no draft query, no waitForEvent) by the mutation-proven `sending.test.ts` + a new explicit guard case. **Note for resumers:** the spec claimed `waitForEvent` was already used in 5b — it was NOT (5b reads `repliedAt` instead); 2d-4 introduces it (Inngest v4.5.0 `step.waitForEvent` verified in-tree). Deploy caveat: in-flight `campaign/lead.start` events emitted before this change lack `leadId`, so a draft step on such a run waits the full 7d then advances without sending (graceful). Gates: `tsc` 0, `vitest` 190 (+9: draft-send 5, sending 4), `next build` green; production code eslint-clean (the `any` in test mocks is the pre-existing project pattern — 4 test files, `npm run lint` was never test-clean). **Phase 2d-3 (per-lead AI email draft: generation + edit) is DONE + verified (2026-06-08): code/test/build + 4-lens adversarial review (0 findings)** (spec `docs/superpowers/specs/2026-06-08-phase-2d-3-email-draft-design.md`, plan `docs/superpowers/plans/2026-06-08-phase-2d-3-email-draft.md`). NOT live-run. **Phase 2d-1 (lead scoring) is DONE + verified (2026-06-08): code/test/build + 3-lens adversarial review** (spec `docs/superpowers/specs/2026-06-08-phase-2d-1-scoring-design.md`, plan `docs/superpowers/plans/2026-06-08-phase-2d-1-scoring.md`). NOT live-run. Scoring = a research-type variant (`kind = SCORING`) that the 2b/2c engine runs to write `score` (0-100, `Lead.score`) + `priority` (1-5) + reasoning (`aiNotes`), surfaced in the lead card / `/leady` (column+sort+min-score filter) / `/pipeline` with manual override. **Phases 2a (ResearchType config CRUD at `/badania`), 2b (research engine: 1 lead, on-demand, async via Inngest), and 2c (batch research: Inngest fan-out over the 2b engine) are DONE + verified at code/test/build + adversarial review** (specs/plans in `docs/superpowers/`, details in the Phase 2 line below). **NOT live-verified in-app** -- no live Anthropic/Inngest research run has been executed; before a real run set `ANTHROPIC_API_KEY` and confirm the `run-research` + `run-research-batch` Inngest functions fire (the user can smoke-test "Uruchom research" on a lead, then "Badanie zbiorcze" on `/leady`). **Phase 2c is DONE + verified (2026-06-08): code/test/build + 5-lens adversarial review** (spec `docs/superpowers/specs/2026-06-08-phase-2c-batch-research-design.md`, plan `docs/superpowers/plans/2026-06-08-phase-2c-batch-research.md`). Full Phase 2 is decomposed 2a->2b->2c->2d in the Phase 2 line below. Phase 7 (visual/polish) stays last. **Phase 6 (sales pipeline) is DONE + verified** (code/test/build green + final holistic review; spec/plan in `docs/superpowers/`, details in the Phase 6 line below). Still open: verify the reply cron on Inngest Cloud before launch. Phases 0/1/3/4, 5a, 5b, 5c and 6 are DONE + verified at the code/test/build level (5b's full live Gmail e2e done; **5c's live e2e: A (SUGGEST) + B (AUTO, incl. fully-automatic cron path) + C (OOO ignored) PASSED; D (bounce) is unit-tested only -- faithful live repro is impractical because Gmail re-threads subject-changed inbounds, see the Phase 5c line**). Deferred 5c items + the flagged poller-idempotency fix live in the "Phase 5c" line below. Gotcha found + fixed during the e2e: the local Inngest dev cron `scan-mailboxes-for-replies` can pile up in QUEUED and never execute (so replies are never scanned) -- restart `inngest-cli dev` fixes it; **verify the cron fires on Inngest Cloud before launch** (reply/opt-out detection depends on it).

## Where we are

| Phase | Scope | Status |
|---|---|---|
| 0 Foundation | Prisma+Neon, Auth.js v5 (Google + allowlist + org membership), app shell, Inngest wired | DONE + verified |
| 1 Leads | OfferingLine, Lead, CSV/paste import (parse+map+dedupe+suppression), leads table, suppression list | DONE + verified |
| 3 Templates+Placeholders+Campaigns | Template library, custom placeholders (+reserved-key guard), pure `renderTemplate` (built-in / `{{zwrot}}` / custom / gendered `{{a\|b}}`), editor with palette + live preview, campaigns + sequence builder + lead assignment, lead edit (honorific + customFields) | DONE + verified |
| 2 Research AI | Multi-type research via Anthropic API + web; in-app research-type config | 2a config + 2b engine + 2c batch + 2d-1 scoring + 2d-2 content + 2d-3 email draft (kind=SCORING/CONTENT/DRAFT) + 2d-4 use draft in sending DONE + verified (2026-06-08): code/test/build + adversarial review (not live-run). Full Phase 2 complete; Phase 7 next |
| 4 Mailbox + sending | Google mailbox connect, Gmail API send, Inngest sending engine | DONE + verified |
| 5 Sequences + replies | Followup delays, reply detection, stop-on-reply, bounce/opt-out | 5a + 5b + 5c DONE (5b: live Gmail e2e done; 5c: inbound classification + keyword/LLM opt-out detector + manual do-not-contact, tsc/test/build green; live e2e A/B/C done, D bounce unit-tested only - live repro impractical (Gmail threading)). Phase 7 next |
| 6 Sales pipeline | `/pipeline` Kanban by `Lead.dealStage` (native DnD), per-lead activity timeline (`LeadActivity` + Message-derived email events), manual notes/logs | DONE + verified (2026-06-08) |
| 7 Visual design / polish | Full UI pass, dashboards, team-role UI, deliverability settings | DONE + verified (2026-06-10) — all 10 sub-projects: design-foundation, sender-signature, dark default + tokens, 13 confirm-guarded actions, CSV custom-field mapping, editable mailbox send-config, pulpit + per-campaign metrics, icon-sidebar shell, page-level-visual-pass (primitives + polonized enum Badges + Card rows), team-role-ui (enforced MEMBER<ADMIN<OWNER ladder, DB invitations, Zespół section). `next build` deferred while dev live |

Verified end-to-end on Neon: auth (User/Account/Membership OWNER), import (dedupe+suppression), templates/placeholders/campaigns, Phase 4 mailbox connect + Gmail send (real email delivered to `SEND_OVERRIDE_TO`, `Message` SENT, `CampaignLead` DONE), and Phase 5a reply detection (real reply on a sent thread -> `CampaignLead` REPLIED + `repliedAt`, inbound `Message` logged). Phase 5b followups verified by unit tests + a mutation-proven orchestrator integration test (`lib/inngest/sending.test.ts`) and a full live Gmail e2e (all 4 scenarios, 2026-06-02). Phase 5c smarter-inbound (bounce/opt-out classification + per-org keyword/LLM detector + manual do-not-contact) verified by unit tests + tsc/build; its live Gmail e2e (bounce + opt-out) is user-run and pending. `tsc` 0, `vitest` 207/207 (Phase 2a +12, Phase 2b +10, Phase 2c +25, Phase 2d-1 +8, Phase 2d-2 +6, Phase 2d-3 +5, Phase 2d-4 +9, Phase 7 sender-signature +8, Phase 7 design-foundation +9), production `next build` green.

## Stack + key decisions (current as of 2026-05-31)

- Next 16.2.6, React 19.2.6, **TypeScript 6**, Tailwind v4, Prisma 7.8 (`prisma-client` generator + `@prisma/adapter-pg`, client at `@/generated/prisma/client`), Auth.js v5 beta (`next-auth` 5 beta.31 + `@auth/prisma-adapter`), **Inngest v4** (triggers inside `createFunction` config; client `isDev: NODE_ENV === "development"`), Zod 4 (`z.email()` not `z.string().email()`), Vitest 4, `@types/node` 22.
- Mechanics-first: minimal raw-Tailwind UI; full visual design is Phase 7.
- Everything org-scoped via `requireOrg()` (`lib/org.ts`); multi-tenant-ready (single org now, no billing/public signup).
- Domain model: `prisma/schema.prisma`. Pure logic lives in `features/*` (e.g. `features/templates/render.ts`, `features/leads/import.ts`, `features/leads/dedupe.ts`) and is unit-tested.

## Standing rules (enforced; see CLAUDE.md / AGENTS.md)

- Conventions: tabs, no semicolons, path comment line 1 (or line 2 after a `"use client"`/`"use server"` directive), `className` first, `import {x}` no inner spaces, NO emoji anywhere.
- Verify current library versions + best practices in the official docs before writing code that uses them.
- Persist knowledge to graphify + Obsidian: `graphify update .` after code changes; `graphify . --obsidian` after doc changes.
- Git: work on `master`; Claude/subagents may commit (no emoji in messages); the USER pushes. Never `git push`.
- Dev server: the USER runs `npm run dev` on :3000. Coordinate `next build` (needs dev OFF) vs live verification (needs dev ON).

## Test data currently in Neon (sample — can be wiped for a clean slate)

**The earlier sample data was wiped** (clean slate) — only the connected `EmailAccount` survived (`snappifyacc@gmail.com`, status CONNECTED, scope incl. `gmail.send`+`gmail.readonly`+`userinfo.email`, window Mon-Fri 08-16). The Phase 5b live e2e recreated a minimal fixture with `scripts/seed-test-campaign.ts`: campaign **Test 5b** (2 sequence steps, both `SEND_IF_NO_REPLY` delay 0 — pass arg `always` to set step 1 `ALWAYS`) + 1 lead (`lead5b@example.com`, honorific PANI) + 2 templates, mailbox wired. Re-run that script any time to reset to a clean PENDING state (it also deletes the lead's `Message`s). Helpers in `scripts/`: `test-mode.ts on|off` (fast-test 24/7+short-gap vs conservative Mon-Fri 08-16 + 180-600s — currently `off`), `test-ids.ts` (read-only state dump incl. `CampaignLead.status`/`currentStep`/`repliedAt`). `SEND_OVERRIDE_TO=nzzhry@gmail.com` (all outbound reroutes there).

## Next steps (detailed)

### Phase 4 — Mailbox + sending (DONE + verified)
- `EmailAccount` model (provider GOOGLE, encrypted OAuth tokens, dailyLimit, send-window, timezone, warmupState, status). Add Campaign sending fields (`sendingEmailAccountId`, `scheduleConfig`).
- Connect a Google mailbox via OAuth (separate from login scope; needs `gmail.send` + read scope for replies later).
- Gmail API send; `Message` model (OUTBOUND, gmailMessageId/threadId, status).
- Inngest v4 sending engine: per-mailbox throttle, send window (e.g. Mon-Fri 8-16), randomized gaps. Single-step send first.
- **Before coding**: verify current Gmail API client + `google-auth-library` + Inngest v4 step/throttle/concurrency patterns in the docs.
- Token encryption at rest; minimal scopes.

**Status (2026-05-31):** Implemented + committed on `master` (plan Tasks 0-11): migration `phase4_mailbox_sending` (`EmailAccount`, `Message`, enums `EmailProvider`/`EmailAccountStatus`/`MessageDirection`/`MessageStatus`, `CampaignLeadStatus` += `SKIPPED`/`FAILED`, `Campaign.sendingEmailAccountId`, `CampaignLead.lastError`); `lib/crypto.ts` AES-256-GCM; pure `features/sending/{recipient,mime,schedule}.ts` + `features/campaigns/activation.ts` (all unit-tested); `features/sending/gmail.ts` send boundary (token-refresh persistence); `features/email-accounts/*` (signed-state OAuth, queries, disconnect); `app/api/mailbox/google/{connect,callback}` routes; `features/campaigns/sending-actions.ts` (`activateCampaign`/`pauseCampaign`/`setSendingMailbox`); `lib/inngest/sending.ts` `sendCampaignEmail` worker (registered in `app/api/inngest/route.ts`); `/skrzynki` page + nav link + campaign sending controls. Gates: `tsc` 0, `vitest` 49/49, `next build` green.
**Live e2e (2026-05-31, done):** GCP OAuth client created, mailbox connected at `/skrzynki` (status `CONNECTED`, tokens stored as ciphertext), seeded campaign activated, real email delivered to `SEND_OVERRIDE_TO`, `Message` logged `SENT`, `CampaignLead` -> `DONE`. Ran with `npm run dev` + the Inngest dev server (`npx inngest-cli dev`). Caveat: the test mailbox's send window was temporarily widened to all-days / 24h for an immediate send -- restore conservative hours (`sendDays`=31 Mon-Fri, `sendWindowStartMin`=480, `sendWindowEndMin`=960) before real campaigns.
**Plan deviations (intentional):** (1) pure `validateActivation` lives in `features/campaigns/activation.ts`, not inside the `use server` `sending-actions.ts` — a `use server` module may only export async functions, and this keeps the unit test off the `next-auth` import chain (which fails to resolve under vitest). (2) The consent screen requests `userinfo.email` alongside `gmail.send` so the connected mailbox address is readable (`gmail.send` alone cannot read it). (3) `planSchedule`'s day-rollover was rewritten so hitting the daily limit rolls to the next valid day (the plan draft stayed same-day); behavior locked by `features/sending/schedule.test.ts`.

### Phase 5 — Sequences + replies (decomposed into 5a / 5b / 5c)

**5a Reply detection + stop-on-reply (DONE + verified).** Committed on `master` (spec `docs/superpowers/specs/2026-05-31-phase-5a-reply-detection-design.md`, plan `docs/superpowers/plans/2026-05-31-phase-5a-reply-detection.md`): migration `phase5a_reply_detection` (`EmailAccount.lastHistoryId`, `CampaignLead.repliedAt`); `gmail.readonly` added to mailbox consent; pure `features/replies/match.ts` (matchReplies, tested); `features/replies/gmail-history.ts` (History API delta boundary, mocked test, 404-expired handling); `features/replies/queries.ts` (tracked threads + atomic recordReply); `lib/inngest/replies.ts` `scanMailboxesForReplies` (5-min cron, fan-out to CONNECTED + readonly mailboxes) + `pollMailboxReplies` (per-mailbox concurrency; `step.sendEvent`/`step.run` durable; baseline/rebaseline cursor; emits `campaign/lead.replied`); `/skrzynki` per-mailbox reply on/off hint. Gates: `tsc` 0, `vitest` 57/57, `next build` green. Mechanism = polling Gmail History API (chosen over watch+Pub/Sub: professional + near-realtime + no 7-day renewal + works on localhost). **Live e2e (2026-06-01, done):** re-consented the mailbox with `gmail.readonly`, activated a fresh send, replied from `nzzhry`; the poller matched the reply on the sent thread -> `CampaignLead` `REPLIED` (+`repliedAt`), inbound `Message` logged, `campaign/lead.replied` emitted. (Debug note: disconnecting a mailbox cascade-deletes its `Message` rows and nulls `Campaign.sendingEmailAccountId`, so a tracked thread requires a send from the *currently* connected mailbox.) Minor: inbound `Message.body` stores the sender `from` (metadata-only fetch, no snippet) — refine when the Phase 6 timeline lands.

**5b Followups / multi-step sequences (DONE + verified).**
- **Goal:** send sequence steps `1..N` (not just step 0) on a delay, honoring each step's `condition`, with stop-on-reply already wired via 5a's `campaign/lead.replied` event.
- **Build on:** `SequenceStep` (`order`, `templateId`, `delayDays`, `condition` = `ALWAYS` | `SEND_IF_NO_REPLY`); `CampaignLead` (`currentStep`, `status`, `repliedAt`). The Phase 4 worker `lib/inngest/sending.ts` `sendCampaignEmail` currently sends ONLY `order: 0` then sets `DONE` (`currentStep=1`). 5a's `pollMailboxReplies` emits `campaign/lead.replied {campaignLeadId}` and sets `REPLIED`.
- **Likely approach (confirm in brainstorm):** after a step sends, schedule the next `SequenceStep` (by `order`) after `step.sleep(delayDays)`. For `SEND_IF_NO_REPLY` steps, race the delay against the reply via `step.waitForEvent("campaign/lead.replied", {timeout: <delayDays>, if/match on data.campaignLeadId})` — reply first => stop (lead already `REPLIED`); timeout => send. Advance `currentStep`; mark `DONE` after the last step. Reuse the send path (render + `sendEmail` + `Message` log). Decide whether to thread followups into the SAME Gmail thread (set `threadId` + `In-Reply-To`/`References` — needs a small `mime.ts`/`gmail.ts` extension) vs new emails.
- **Open design questions for brainstorm:** one long-lived durable function looping all steps vs event-per-step; how the mailbox daily-limit/send-window applies to followups; threading headers; behavior of in-flight followups on pause / reply / disconnect.
- **Process:** brainstorming skill -> spec in `docs/superpowers/specs/` -> plan in `docs/superpowers/plans/` -> subagent-driven execution (same as 5a). Branch: work on `master`; the USER pushes.

**Status (2026-06-01, DONE + verified).** Spec `docs/superpowers/specs/2026-06-01-phase-5b-followups-design.md`, plan `docs/superpowers/plans/2026-06-01-phase-5b-followups.md`. **No migration.** New pure `features/sequences/plan.ts` (`decideStep`/`planNext`, tested) + `features/sending/schedule.ts` `scheduleFollowupSlot` (tested). `lib/inngest/sending.ts`: `sendCampaignEmail` replaced by the durable per-lead orchestrator `runLeadSequence` (loops steps `1..N`; per-step `condition` + stop-on-reply via `repliedAt`; window/limit/suppression gates reused; **standalone followup emails**, no Gmail threading; `concurrency {limit:1,key:emailAccountId}` throttles the send step while inter-step `step.sleepUntil` holds no slot; `cancelOn campaign/paused` + `singleton {key:campaignLeadId,mode:"cancel"}`); body extracted as `runLeadSequenceHandler` for testability. `features/campaigns/sending-actions.ts`: activation emits `campaign/lead.start {campaignLeadId,campaignId,emailAccountId}` and **resumes in-flight leads**; `pauseCampaign` emits `campaign/paused`; `validateActivation` gained `inflightLeadCount`. Status lifecycle: lead stays `ACTIVE` between steps; `REPLIED` wins over `DONE`. Gates: `tsc` 0, `vitest` 75/75 incl. a mutation-proven integration test (`lib/inngest/sending.test.ts`: followup sends; SEND_IF_NO_REPLY skip on reply; ALWAYS-after-reply still sends; pause stops; resume-from-currentStep), `next build` green. Inngest v4.5.0 verified in-tree: `concurrency.limit` caps concurrent STEPS (sleeping runs free); `cancelOn match` is deprecated -> used `if`; `singleton mode:"cancel"`. **Deltas from the plan-as-written:** `cancelOn` uses `if`; `HARD_STOP` includes `PENDING`+`DONE`; `finalizeLead` excludes `DONE`; `activateCampaign` rejects re-activating an `ACTIVE` campaign; loop-exhaustion path finalizes. **Live Gmail e2e DONE (2026-06-02):** all 4 scenarios verified on the real mailbox (via `scripts/seed-test-campaign` + `scripts/test-mode` + the Inngest dev panel) — A followup (2 sends -> DONE), B stop-on-reply (SEND_IF_NO_REPLY skipped -> REPLIED, 1 send), C ALWAYS-after-reply (2 sends + REPLIED), D pause cancels the in-flight run (`cancelOn`) then re-activate resumes -> DONE. Reusable test helpers in `scripts/` (seed/reset/test-mode/ids); mailbox restored to conservative send settings afterwards. **Phase 4 retry-stomp bug FIXED (2026-06-08, commit `a40960a`):** the inline catch no longer sets `FAILED` (which is HARD_STOP and bailed the very next retry at the status gate); send/log errors now propagate so Inngest actually retries, and `FAILED` is set by an `onFailure` handler (extracted as `handleLeadSequenceFailure`, unit-tested) only after `retries:3` are exhausted, guarded so it never stomps a terminal status.

**5c Smarter inbound: bounce + opt-out + RODO (DONE — code/test/build + live e2e A/B/C; D unit-tested only).** Spec `docs/superpowers/specs/2026-06-02-phase-5c-bounce-optout-design.md`, plan `docs/superpowers/plans/2026-06-02-phase-5c-bounce-optout.md`. Migration `phase5c_inbound_classification` (enums `OptOutMode`/`OptOutDetector`/`InboundKind`; `Organization.optOut*`; `Message.inboundKind`). Pure `features/replies/classify.ts` (bounce/auto-reply/reply, tested) + pluggable opt-out detector `features/replies/optout/*` (keyword default + Anthropic LLM `claude-haiku-4-5`, fail-safe; `getDetector` switch). `gmail-history.ts` fetches snippet + `Auto-Submitted`/`Precedence`/`X-Failed-Recipients`. `queries.ts` `recordInbound` (replaced `recordReply`): per-kind atomic writes -> bounce = `BOUNCED` and **NOT** a reply (fixes the 5a bug where bounces counted as replies), OOO = ignored, reply = `REPLIED` + runs the detector per org `optOutMode` (OFF/SUGGEST/AUTO; AUTO auto-suppresses). `lib/inngest/replies.ts` poller emits `campaign/lead.replied` only for REPLY/OPT_OUT_SUSPECT; `getTrackedThreads` excludes BOUNCED. Per-org settings at `/ustawienia`; manual `markDoNotContact` + leads/replies list (`lead-inbox.tsx`) on the campaign page. Suppress/bounce -> `UNSUBSCRIBED`/`BOUNCED` (in `runLeadSequence` HARD_STOP) so in-flight followups self-stop. Sender identity = template content (no feature). Gates: `tsc` 0, `vitest` 99/99 (classify 12, keyword 5, llm 7), `next build` green. **Live e2e (2026-06-03): A (opt-out SUGGEST) + B (opt-out AUTO) PASSED.** B was verified via the fully-automatic cron path (cron -> fan-out -> poll -> classify REPLY -> keyword detect -> auto-suppress, with no click and no manual trigger; lead went UNSUBSCRIBED at currentStep 1 so krok-1 self-stopped). **C (OOO ignored) PASSED 2026-06-08** (lead reached DONE through both steps with `repliedAt` null -- OOO did not stop the sequence). **D (bounce): live repro abandoned, unit-tested only.** Key lesson (worth remembering): Gmail re-threads a reply whose SUBJECT changed (e.g. prepending "Automatyczna odpowiedź:") onto a NEW `threadId`, so the OOO lands on an untracked thread and the poller correctly ignores it; real autoresponders/bounce DSNs are caught via `Auto-Submitted`/`Precedence`/subject which a manual Gmail Reply cannot faithfully set without splitting the thread -- so the AUTO_REPLY + BOUNCE classify branches are validated by `classify.test.ts` (12 cases), while the live e2e validates the OUTCOME (OOO does not stop the sequence) + the reply/opt-out path (A/B). Dev-server gotcha found + fixed: `scan-mailboxes-for-replies` cron runs piled up in QUEUED and never executed (replies never scanned) until `inngest-cli dev` was restarted -- the 5c pipeline is correct (proven end-to-end), this is a dev-server artifact; **verify the cron executes on Inngest Cloud before launch.** Diagnostics added in `scripts/diag-*.ts` (notably `diag-trigger-poll.ts` to force a poll without waiting for the cron, `diag-5c.ts` state dump, `diag-reset-suppression.ts` to clear the lead5b suppression between runs since seed does not). **Scenario behaviors:** bounce on a tracked thread -> `BOUNCED` + badge (not replied); reply "nie jestem zainteresowany" with mode SUGGEST -> "możliwa rezygnacja" badge -> click "Nie kontaktować" -> `Suppression` + `UNSUBSCRIBED`; AUTO auto-suppresses; OOO -> sequence continues. **Deferred:** unsubscribe link + `List-Unsubscribe`; bounce auto-suppress; custom AI detector (SUGGEST confirmations = its training set). **Poller idempotency FIXED (2026-06-08, commit `d9d7056`):** `recordInbound` early-returns if an INBOUND `Message` with that `gmailMessageId` already exists -> idempotent on Inngest at-least-once step retry. Deliberately NO DB unique constraint (the OUTBOUND `log-${order}` step shares `gmailMessageId` and would break on its own retry under a global unique); the poller's `concurrency {limit:1}` serialization makes the check-then-write race-free. **Detector model:** `claude-haiku-4-5` (cheap, per-reply) — switch to `claude-opus-4-8` in `optout/llm.ts` for max accuracy (needs `ANTHROPIC_API_KEY`).

### Phase 2 — Research AI (decomposed; 2a + 2b DONE 2026-06-08, build 2c next)
**Decision:** rather than stay parked on hand-authored prompts, make research prompts/types **in-app data** (user-editable), then build the engine on top. Full Phase 2 is multiple subsystems -- too big for one spec -- so it is decomposed into sub-projects (each its own brainstorm -> spec -> plan -> build), in order:
- **2a Research-type config (DONE + verified 2026-06-08):** spec `docs/superpowers/specs/2026-06-08-phase-2a-research-types-design.md`, plan `docs/superpowers/plans/2026-06-08-phase-2a-research-types.md`. Built brainstorm (with visual companion) -> spec -> plan -> inline TDD -> adversarial review. Migration `phase2a_research_types` (`ResearchType` org-scoped: `name` unique-per-org, `prompt`, `outputFields Json`, `modelId`, `webSearchEnabled`). Pure `features/research-types/targets.ts` (Lead-column allow-list -> derived/locked types; honorific=GENDER, `email` special/collision-deferred-to-2b, `dealStage` forbidden) + `schema.ts` (Zod `outputFields` contract: per-field + cross-field refines -- bad key, reserved CUSTOM key, forbidden target, type/target mismatch, GENDER-only-honorific, unique keys, one non-CUSTOM target each, min 1 field), 12 unit tests. `actions.ts` (use-server create/update/delete, org-scoped, dup-name caught) + `queries.ts`. Pages `/badania` list/new/edit + card editor `research-type-form.tsx` (target derives+locks type, CUSTOM unlocks; stable row keys; `outputFields` JSON-serialized; `useActionState` + `router.push` on ok) + nav link. **Decisions:** flat `Json` storage (no `OutputField` table), no versioning (2c snapshots the definition at queue time), hard delete, org-wide CRUD, plain-text prompt rendered per-lead in 2b via the `{{token}}` engine, curated `modelId` dropdown. Gates: `tsc` 0, `vitest` 127, `next build` green (`/badania`, `/badania/[id]`, `/badania/nowy`). Review: org-scoping airtight + Next/React idioms clean; fixed an index-as-key bug in the editor. **NOT live-verified in-app** (2a has no API/Inngest -- the user can smoke-test the CRUD at `/badania`).
- **2b Research engine (single lead, on-demand) -- DONE + verified 2026-06-08 (code/test/build + 6-dimension adversarial review; NOT live-run):** spec `docs/superpowers/specs/2026-06-08-phase-2b-research-engine-design.md`, plan `docs/superpowers/plans/2026-06-08-phase-2b-research-engine.md`. Async via Inngest (`run-research` fn in `lib/inngest/research.ts`, event `research/run.requested`, per-org concurrency, retries:2). Migration `phase2b_research_runs` (`ResearchRun`: status QUEUED/RUNNING/DONE/FAILED, mode AUTO/MANUAL, findings/diff Json, `researchTypeId` SetNull + `researchTypeName` snapshot; `LeadActivityKind += RESEARCH`). Mechanism: unified `submit_findings` strict tool + `web_search_20260209`/`web_fetch_20260209` when enabled (structured-output `output_config.format` is 400-incompatible with web-search citations). Pure `features/research/{schema-build,apply,prompt,model,types}.ts` (10 unit tests): schema-gen, coercion (gender->PAN/PANI normalize, siteQuality/priority clamp 1-5, email validate, CUSTOM->string, customFields MERGE not replace), AUTO fill-empty / MANUAL all-proposed, default model `claude-opus-4-8` + effort/thinking gated off for haiku. `engine.ts` Anthropic boundary (agentic loop, pause_turn resume, fail-safe). `actions.ts` start/poll/confirm (org-scoped, email collision pre-check). Lead-page UI `uruchom-research.tsx` (type picker + AUTO/MANUAL toggle, poll, diff with confirm). Review fixes: cross-org guard in the handler, `strict:true`, apply error handling, max_tokens 16000. Gates: `tsc` 0, `vitest` 137, `next build` green. **NOT live-run** -- set `ANTHROPIC_API_KEY` + confirm the `run-research` Inngest function fires before trusting results.
- **2c Batch research (Inngest fan-out) -- DONE + verified 2026-06-08 (code/test/build + 5-lens adversarial review; NOT live-run):** spec `docs/superpowers/specs/2026-06-08-phase-2c-batch-research-design.md`, plan `docs/superpowers/plans/2026-06-08-phase-2c-batch-research.md`. Migration `phase2c_research_batches` (`ResearchBatch` model org-scoped; `ResearchRun` += `batchId` + snapshot fields `promptSnapshot`/`outputFieldsSnapshot`/`webSearchEnabledSnapshot`/`modelIdSnapshot`; `ResearchRunStatus += CANCELLED`; new `ResearchBatchStatus`). Pure (unit-tested): `features/research/batch-select.ts` (`buildLeadWhere`), `batch-dedupe.ts` (`partitionCandidates`/`capBatch`, MAX_BATCH 200), `batch-progress.ts` (`deriveBatchStatus` -- read-time progress, no write-back). Shared `apply-confirm.ts` `applyProposedToLead` extracted from `confirmResearchApply` (2b confirm + 2c batch share it). Durable orchestrator `lib/inngest/research-batch.ts` `runResearchBatchFn` (event `research/batch.requested`, `{limit:1,key:org}`, `cancelOn research/batch.cancelled`, retries:2): org-scoped batch load, dedup+cap, `createMany` runs, `step.sendEvent` fan-out `research/run.requested`; all batch writes wrapped in `step.run`. 2b engine changes (shared, behaviorally identical for single runs): `engine.ts` transient errors (429/5xx/529/connection) **rethrow -> Inngest retry**, permanent (billing/400) -> `{ok:false}` FAILED; `runResearchFn` snapshot-read (per-run snapshot preferred, else live type), skip-CANCELLED, `onFailure` marks FAILED, retries 3. Actions `batch-actions.ts` (`previewBatch`/`startBatch`/`getBatch`/`cancelBatch`/`resumeFailed`/`applyBatch`, org-scoped) + `batch-queries.ts`. UI: "Badanie zbiorcze" launcher on `/leady` (preview matched/skipped/willQueue + confirm), batch page `/badania/batches/[id]` (self-scheduling 2s poll, per-lead table, MANUAL bulk/per-row approve, cancel/resume-failed), recent-batches list on `/badania`. Guards: MAX_BATCH 200, 30-day re-research dedup (`includeRecent` override, persisted as a column), preview-confirm, cancel. **Deviations from spec:** `includeRecent` persisted as a `ResearchBatch` column (orchestrator needs it at execution); recent-batches list added on `/badania` (reachability). Review fixes: orchestrator org-scoping + `step.run`-wrapped batch writes (idempotent on retry); cancel/resume update where-clauses org-scoped; poll rewritten to self-scheduling `setTimeout` (no stacked requests) + local `refresh()` after actions. **Accepted trade-offs (not bugs):** createMany-then-load-ids double-insert on a rare partial-retry (AUTO fills only empty fields so benign); `inngest.send` outside the DB tx in server actions (same pattern as `startBatch`). Gates: `tsc` 0, `vitest` 162 (+25: batch-select 5, batch-dedupe 4, batch-progress 6, apply-confirm 4, research-batch 6), `next build` green (`/badania/batches/[id]`). **NOT live-run** -- set `ANTHROPIC_API_KEY` + smoke-test one lead (2b) before a large batch; confirm `run-research-batch` fires on Inngest Cloud.
- **2d Scoring/priority + content writing (decomposed into 2d-1 + 2d-2):**
  - **2d-1 Lead scoring -- DONE + verified 2026-06-08 (code/test/build + 3-lens adversarial review; NOT live-run):** spec `docs/superpowers/specs/2026-06-08-phase-2d-1-scoring-design.md`, plan `docs/superpowers/plans/2026-06-08-phase-2d-1-scoring.md`. Scoring is a research-type **variant** via `ResearchType.kind` (`RESEARCH` | `SCORING`); the AI returns `score` (0-100, new `Lead.score`) + `priority` (1-5, existing) **both independently** (no derivation) + reasoning (`aiNotes`). Migration `phase2d_scoring` (additive: `ResearchTypeKind` enum + `ResearchType.kind` + `Lead.score`). New `score` output target (clamp 0-100) in `targets.ts`/`apply.ts`; Zod refine "SCORING requires a score output" (`schema.ts`); `kind` persisted in `research-types/actions.ts`. Pure `features/leads/score-fields.ts` `parseScoreFields` (clamp score 0-100 / priority+siteQuality 1-5, empty/non-numeric -> null) wired into `updateLead` (org-scoped). Engine/run/batch (2b/2c) reused unchanged. UI: kind selector + `[Ocena]` badge on `/badania`; lead card shows + edits score/priority/siteQuality/aiHook/aiNotes (manual override; AUTO fill-empty preserves a manual value); `/leady` Score column + `sort=score` (nulls last) + `minScore` filter (list-only, NOT threaded into batch criteria -- hook for 2d-2); `/pipeline` score badge; `[Ocena]` labels in run/batch pickers. Also fixed a pre-existing `<a>`->`<Link>` lint error in `uruchom-research.tsx`. Gates: `tsc` 0, `vitest` 170 (+8: targets 1, apply 1, schema 3, score-fields 3), `next build` green, eslint clean. **Review:** 1 finding (priority-in-pipeline-select) assessed false-positive -- the card shows only the score badge per design, `priority` not needed there. **NOT live-run** -- needs `ANTHROPIC_API_KEY`; smoke-test a SCORING type on one lead.
  - **2d-2 Content writing -- DONE + verified 2026-06-08 (code/test/build + 3-lens adversarial review; NOT live-run):** spec `docs/superpowers/specs/2026-06-08-phase-2d-2-content-writing-design.md`, plan `docs/superpowers/plans/2026-06-08-phase-2d-2-content-writing.md`. Research-type variant `ResearchTypeKind += CONTENT`; CONTENT types output `aiHook` and/or `customFields` (existing targets). Migration `phase2d2_content` = enum value only. Pure `features/research/lead-context.ts` `buildLeadContextBlock(lead)` (non-empty aiNotes/score/siteQuality/region/schoolType/contactRole/email/phone/customFields; "" when nothing known) appended to the prompt in `runResearchFn` **only when `researchType.kind === "CONTENT"`** (kind read live, no snapshot -- mid-batch kind edit is the only edge); RESEARCH/SCORING byte-identical (block ""). Model decides whether to web-search (existing `webSearchEnabled`). Zod refine "CONTENT must output an aiHook or CUSTOM field". UI: controlled kind selector += CONTENT with a reactive per-kind hint, "Treść" badge on `/badania`, `[Treść]` picker labels; editing (aiHook + customFields) + diff + run/batch reused. Gates: `tsc` 0, `vitest` 176 (+6: lead-context 3, schema 3), `next build` green, eslint clean. **Review:** 1 real finding (missing CONTENT hint) fixed (controlled select + per-kind hint); rest refuted/cosmetic. **NOT live-run** -- needs a funded `ANTHROPIC_API_KEY`; run a RESEARCH type first so the context block has material.
  - **2d-3 Per-lead AI email draft (generation + edit) -- DONE + verified 2026-06-08 (code/test/build + 4-lens adversarial review, 0 findings; NOT live-run):** spec `docs/superpowers/specs/2026-06-08-phase-2d-3-email-draft-design.md`, plan `docs/superpowers/plans/2026-06-08-phase-2d-3-email-draft.md`. New `LeadDraft` model (one per lead, own `DraftStatus`; migration `phase2d3_drafts` additive) + `ResearchTypeKind += DRAFT`. Draft generation = a `kind=DRAFT` research type; a NEW isolated `lib/inngest/draft.ts` `runDraftFn` REUSES the existing `runResearch` engine with a fixed `{subject,body}` schema (pure `features/research/draft.ts` `buildDraftSchema`/`coerceDraft`) + the `buildLeadContextBlock` prior-research block + a copywriting `system` override (the ONLY engine change: an additive optional `system` param on `runResearch`, default unchanged). Org-scoped actions `features/research/draft-actions.ts` (`startDraft`/`getDraft`/`saveDraft`, upsert on `@@unique([organizationId,leadId])`). UI: `draft-panel.tsx` on the lead card (pick DRAFT type -> Generuj -> poll -> edit subject/body -> Zapisz); DRAFT option + per-kind hint + hidden output-fields editor (submits `[]`) in the config form; "Draft" badge on `/badania`; DRAFT excluded from the research-run picker + batch launcher, shown only in the draft panel. Schema: `outputFields` now optional (`.default([])`) with a refine "non-DRAFT requires >=1 field". Gates: `tsc` 0, `vitest` 181 (+5: draft 3, schema 2), `next build` green, eslint clean. **Isolation:** no change to `lib/inngest/sending.ts`, `runResearchFn`, or the batch orchestrator. **Using the draft when sending = 2d-4.** Batch draft generation also deferred. **NOT live-run** (needs a funded `ANTHROPIC_API_KEY`).
  - **2d-4 Use the draft in sending -- DONE + verified 2026-06-08 (code/test/build; NOT live-run):** spec `docs/superpowers/specs/2026-06-08-phase-2d-4-send-draft-design.md`, plan `docs/superpowers/plans/2026-06-08-phase-2d-4-send-draft.md`. `SequenceStep += useLeadDraft Boolean` (migration `phase2d4_send_draft`, additive; `templateId` stays required, unused for draft steps); sequence-builder checkbox + step marker. Worker `lib/inngest/sending.ts`: in the SEND branch, `resolveDraftDecision(useLeadDraft, draft)` (pure `features/sequences/draft-send.ts`) -> `template` (unchanged render) / `draft` (send `LeadDraft.subject`/`body` **verbatim**, no token render) / `wait` (`step.waitForEvent("research/draft.ready", {timeout:"7d", if:"async.data.leadId == event.data.leadId"})` -> re-load + re-decide -> send if now ready, else advance without sending). `research/draft.ready {leadId, organizationId}` emitted by `runDraftFn` (on DONE) + `saveDraft`; `campaign/lead.start` gained `leadId`. **Invariant held: the `useLeadDraft===false` path is byte-identical** (no draft query, no waitForEvent) -- regression-guarded by the mutation-proven `sending.test.ts` + a new explicit guard case. **Implementation notes:** the spec wrongly claimed `waitForEvent` was already used in 5b -- it was NOT (5b reads `repliedAt`); 2d-4 is the first `step.waitForEvent` in-tree (Inngest v4.5.0 signature `waitForEvent(id, {event, timeout, if})` returning the event or `null` on timeout, verified). `campaign/lead.start`'s planner field `p.leadId` is the *campaignLead* id (existing naming) -- the new event `leadId` is the real `Lead.id`, looked up from the selects. Deploy caveat: pre-change in-flight `campaign/lead.start` events lack `leadId`, so a draft step there waits the full 7d then advances (graceful). Gates: `tsc` 0, `vitest` 190 (+9: draft-send 5, sending 4), `next build` green; production code eslint-clean. Optional later: batch draft generation (fan-out like 2c). NOT live-run -- needs a funded `ANTHROPIC_API_KEY` (drafts) + a live e2e before trusting in production.
First research type (domain reference): messages/licea -> full name, director first name + **gender**, email (director -> secretariat fallback). Gender feeds `honorific`; results map to lead fields + `customFields`.

### Phase 6 — Sales pipeline (DONE + verified, 2026-06-08)
Spec `docs/superpowers/specs/2026-06-08-phase-6-sales-pipeline-design.md`, plan `docs/superpowers/plans/2026-06-08-phase-6-sales-pipeline.md`. Built brainstorming -> spec -> plan -> subagent-driven execution (10 tasks, fresh subagent per task + spec/quality review per task + final holistic review). Migration `phase6_pipeline`: new `LeadActivity` model (+ enum `LeadActivityKind` NOTE/CALL/MEETING/OFFER_SENT/STAGE_CHANGE; org-scoped; cascade with Lead/Org). Pure logic in `features/pipeline/` (unit-tested): `group.ts` `groupLeadsByStage`; `timeline.ts` `messageToTimelineItem`/`activityToTimelineItem`/`buildTimeline` (email events DERIVED from `Message` at read time, NOT duplicated — Phase 4/5 untouched); `validation.ts` `addActivitySchema`. `queries.ts` `getPipelineLeads`/`getLeadTimeline`. `actions.ts` (`use server`, org-scoped): `moveLeadStage` (atomic `$transaction`: stage update + STAGE_CHANGE log), `addLeadActivity` (Zod; STAGE_CHANGE not user-creatable), `deleteLeadActivity` (refuses STAGE_CHANGE = immutable history). UI: `/pipeline` (server page + client `board.tsx`/`card.tsx`, native HTML5 drag-and-drop to change stage, offering-line filter, collapsible WON/LOST, soft per-column cap 60 + "pokaż więcej") + nav link; lead detail `/leady/[id]` gained the activity timeline + "Dodaj wpis" form. Gates: `tsc` 0, `vitest` 109/109, `next build` green (`/pipeline` + `/leady/[id]` in the route list). Final review: ready to merge, no critical/important issues (org-scoping airtight on every read/write; stage history immutable). **Deltas from spec:** card sequence-status orders `CampaignLead` by `createdAt` (no `updatedAt` column exists); WON/LOST columns default collapsed ALWAYS (spec said "when empty") — intentional, count still shown in the header. **Deferred to Phase 7:** drag-drop polish (touch/animation/optimistic), owner-assignment UI, dashboards/metrics (sent/reply/won rates), structured lost-reason field.

### Phase 7 — Visual design / polish (decomposed 2026-06-09; in progress)
**Inventory + decomposition (workflow, 2026-06-09):** the app is fully functional but visually bare ("mechanics-first"): no design system (globals.css is just `@import "tailwindcss"`, no `@theme`/tokens, no shared `components/`/`ui/` primitives, `lib/cn.ts` unused), no loading/error/empty/not-found states, raw enum strings shown to users, mixed PL/EN copy, flat 10-link nav with no active state/mobile, and several destructive actions firing on a single click with no confirmation. No home/dashboard (`/` redirects to `/leady`). Decomposed into **9 independently-shippable sub-projects** (recommended order):
1. **`design-foundation`** — **DONE + verified 2026-06-09** (code/test/build; full summary in the NEXT TASK line above). `@theme` tokens (dark+light, runtime default light) + `components/ui/*` primitives + scaffolding (Toast, ConfirmDialog, route boundaries, Container, a11y) + Motion fluid-body; `/ustawienia` rebuilt as the reference surface. 8/9 sub-projects build on it.
2. **`sender-signature`** (S, objective) — **DONE + verified 2026-06-09** (see below).
3. **`destructive-action-safety`** — **DONE + verified 2026-06-09** (code/test/build green; see the dedicated line below). `ConfirmButton` primitive + 13 confirm-guarded actions.
4. **`import-customfields-mapping`** — **DONE + verified 2026-06-09** (code/test/build green; see the dedicated line below). Column->target import wizard sends arbitrary CSV columns to `Lead.customFields`; remove-row button on the lead-detail editor.
5. **`deliverability-settings`** — **DONE + verified 2026-06-09** (code/test/build green; see the dedicated line below). Editable per-mailbox send-config form on `/skrzynki` + `updateEmailAccountSettings`. (SPF/DKIM/DMARC guidance + a health section were scoped OUT — config-only by user choice — and remain a later add.)
6. **`dashboards`** — **DONE + verified 2026-06-09** (code/test/build green; see the dedicated line below). Pulpit at `/` (KPIs + sales funnel + campaigns table) + per-campaign metrics strip; new `features/metrics` (compute + queries); open/click omitted (no tracking model).
7. **`shell-and-nav-redesign`** — **DONE + verified 2026-06-09** (code/test/build green; see the dedicated line below). Icon sidebar (variant C, collapses to icons) + grouped sections + left-bar active state + account menu (user/org/role + Wyloguj).
8. **`page-level-visual-pass`** — **SPECCED 2026-06-09** (direction chosen via mocks; ready to PLAN + EXECUTE in a fresh session; see the dedicated line below). Card rows for library lists + full polonization + a shared `features/enums/labels.ts` + primitive adoption across ~25 surfaces (audit: 131 raw sites / 27 enum leaks / 32 copy issues). Dark-correctness was already done by the dark-ready migration; this owns the deeper polish.
9. **`team-role-ui`** (L, low, mixed, deps: 1) — members/roles surface; role is returned by `requireOrg` but NEVER enforced (no `requireRole`); no Invitation model — needs a user decision on the invite mechanism. Deferred (single-operator org today).

**Sub-project 1 (build order #2) — `sender-signature` — DONE + verified 2026-06-09 (code/test/build; NOT live-run, but pure code/data+UI so no live run needed):** spec `docs/superpowers/specs/2026-06-08-phase-7-sender-signature-design.md`, plan `docs/superpowers/plans/2026-06-08-phase-7-sender-signature.md`. Built via subagent-driven-development (7 tasks, fresh implementer + spec-compliance + code-quality review each). Fixes the verified preview-vs-send `{{podpisNadawcy}}` divergence (preview used `session.user.name`, send used `account.displayName`). Migration `phase7_sender_signature` (additive `Organization.senderSignature String @default("")`). Pure `features/templates/sender.ts` `resolveSenderName({signature, fallback})` = signature-trimmed-if-set else fallback-trimmed else "" + `sanitizeSignature` (trim + 2000 cap) — the SINGLE source, called by both `lib/inngest/sending.ts` (fallback = `account.displayName`) and `app/(app)/szablony/page.tsx` (fallback = first CONNECTED mailbox `displayName`). Empty signature -> mailbox-name fallback, **flagged** in `/ustawienia` (`setSenderSignature` action + `SenderSignatureForm`, separate from the opt-out form) and the editor preview note. `render.ts` unchanged (`{{podpisNadawcy}}` already reads `ctx.senderName`). Gates: `tsc` 0, `vitest` 198 (+8: sender 6, render 2), `next build` green, eslint-clean (prod). Behavior note: until the signature is set, preview shows the connected-mailbox name (flagged) and send uses the mailbox name (unchanged) — set it once and preview == send.

**Sub-project (build order #3) — `dark-ready token migration` — DONE + verified 2026-06-09 (code/test/build; NOT live-eyeballed — pure class-name swap, statically verified):** spec `docs/superpowers/specs/2026-06-09-phase-7-dark-ready-migration-design.md`, plan `docs/superpowers/plans/2026-06-09-phase-7-dark-ready-migration.md`. A focused, objective subset of `page-level-visual-pass`: mechanically swapped every literal `zinc-*`/`white`/status color class -> the design-foundation token utilities across all **33 pages** (one shared canonical map + 8 resolved exceptions), then flipped the runtime default theme light->dark. Built via a fan-out workflow (7 disjoint slices, each migrated by one agent then independently audited-and-corrected by a fresh agent — 0 corrections needed) + main-loop gates/commits. Per-slice commits (login / leady / import-lines-suppression / kampanie / badania / szablony-placeholdery / skrzynki-pipeline) + the default-flip commit. Exceptions held: solid amber CTAs (`bg-amber-600 text-white` on draft-panel/uruchom-research/batch-view) -> `bg-primary text-primary-fg`; suppression "Usuń" -> `text-danger`; `leady` table-header bottom border -> `border-border-strong`; the research-KIND category chips (`bg-amber-100`/`bg-sky-100`/`bg-violet-100` on `badania/page.tsx` + the violet draft badge on `sequence-and-leads.tsx`) left AS-IS as readable light tints — the ONLY literals remaining (a guard grep returns exactly those 4 lines). Flip = `features/theme/resolve.ts` default `light`->`dark` + the mirrored `app/layout.tsx` no-flash script + `<html data-theme="dark">` + `resolve.test.ts`. Gates: `tsc` 0, `vitest` 207, `eslint` clean (`app/(app)`), `next build` green. Remaining Phase 7 taste-work (`shell-and-nav-redesign`, `page-level-visual-pass`) now owns only the deeper polish (primitive adoption, spacing/copy, dark-native category-badge tokens).

**Sub-project (build order #4) — `destructive-action-safety` — DONE + verified 2026-06-09 (code/test/build; NOT live-eyeballed — UI glue, statically verified):** spec `docs/superpowers/specs/2026-06-09-phase-7-destructive-action-safety-design.md`, plan `docs/superpowers/plans/2026-06-09-phase-7-destructive-action-safety.md`. Built brainstorm -> spec -> plan -> subagent-driven fan-out (Task 1 primitive inline, then 3 adoption groups, each independently audited-and-corrected — 0 corrections). New client primitive `components/ui/confirm-button.tsx` `<ConfirmButton action confirm toast variant size>`: on click awaits the existing `useConfirm()` dialog, runs the bound server action in a `useTransition` (pending -> disabled + `Spinner`), then fires `useToast`. First real adoption of the `useConfirm`/`useToast` primitives (defined in design-foundation, previously unused). Guards all 13 destructive actions: 12 `<form action={X.bind}>` -> `<ConfirmButton>` swaps (deletes: research-type, placeholder, offering-line, template, campaign, lead, lead-activity, sequence-step, suppression-entry; + disconnect-mailbox, pause-campaign, mark-do-not-contact) and the `cancelBatch` client handler gated in-place with `useConfirm` (it returns a `BatchActionResult`, so the primitive does not fit). Deletes/disconnect use `variant=danger` + `confirm.danger=true`; pause/cancel use a neutral `secondary` trigger + non-danger confirm. Concise PL confirm copy + per-site success toasts (spec table). Excluded by design (documented): the research-type-form unsaved remove-field (local form state) + sign-out. All target server actions already `revalidatePath`, so calling them from a client transition refreshes exactly like the prior form submit (verified per action). Gates: `tsc` 0, `vitest` 207 (unchanged — `ConfirmButton` is glue, no extractable pure logic), `eslint` clean (`app/(app)` + `components`), `next build` green; a guard grep confirms zero destructive `<form>` remain.

**Sub-project (build order #5) — `import-customfields-mapping` — DONE + verified 2026-06-09 (code/test/build):** spec `docs/superpowers/specs/2026-06-09-phase-7-import-customfields-mapping-design.md`, plan `docs/superpowers/plans/2026-06-09-phase-7-import-customfields-mapping.md`, visual mock `docs/superpowers/design/import-mock/index.html`. Built brainstorm (offered a dark-themed visual companion comparing Wariant A column->target vs Wariant B "rest as custom" checkbox — user picked A) -> spec -> plan -> inline TDD + adversarial code review. The CSV importer no longer drops unknown columns: each column gets one target (ignore / a known lead field / a custom field with an editable key) and custom columns land in `Lead.customFields`. Pure `features/leads/import.ts` (TDD, +5 cases): `buildImportSpec(ColumnTarget[]) -> {mapping, customColumns}` (known-field last-wins; custom keys trimmed, empty dropped), `defaultCustomKey(header,index)` (trimmed header else `kolumna_N`), `customFields: Record<string,string>|null` added to `LeadImportInput`, and `mapRowsToLeads` builds the custom bag per row (skips empty cells, null when none). `importLeads` parses per-column `target_<i>`/`customkey_<i>` form fields via `buildImportSpec`; `createMany` coerces `customFields: l.customFields ?? undefined` so the Prisma `Json?` column gets SQL NULL (never a literal `null`, which Prisma rejects for nullable Json). Wizard `import-wizard.tsx` rewritten column-centric (controlled `targets` keyed by column index; one `<select>` per column + a dashed custom-key input; resets the mapping when the column COUNT changes so a re-paste can't silently mis-map — the review's one important finding). Remove-row `×` button added to the lead-detail customFields editor (`lead-edit-form.tsx`; no confirm — local unsaved edit, same class excluded in `destructive-action-safety`). `dedupe.test.ts` fixture updated for the new required field. No migration (`Lead.customFields Json?` already existed). Gates: `tsc` 0, `vitest` 212 (+5), `eslint` clean (`app/(app)` + `features` + `components`), `next build` green. Review: 1 important (fixed), 2 minor accepted (unvalidated `field:` cast is inert because `mapRowsToLeads` only reads the 10 known keys; hasHeader key-snapshot is defensible).

**Sub-project (build order #6) — `deliverability-settings` — DONE + verified 2026-06-09 (code/test/build):** spec `docs/superpowers/specs/2026-06-09-phase-7-deliverability-settings-design.md`, plan `docs/superpowers/plans/2026-06-09-phase-7-deliverability-settings.md`, visual mock `docs/superpowers/design/deliverability-mock/index.html`. Built brainstorm (rendered a dark-themed mock of the full design; the user chose **config-only** + a curated timezone select) -> spec -> plan -> inline TDD + adversarial code review. Gives each connected mailbox an editable UI for the send-pacing config the engine already reads but had no editor for (`/skrzynki` previously showed only a read-only `dailyLimit`). Pure `features/email-accounts/settings.ts` (TDD, +10 cases): `parseTime`/`formatTime` ("HH:MM" <-> minutes), `maskToDays`/`daysToMask` (the `sendDays` bitmask, bit0=Mon..bit6=Sun), `isValidTimezone` (try `Intl.DateTimeFormat`), and `validateEmailAccountSettings` (dailyLimit 1-1000; window start<end within 0..1440; gaps integers 0<=min<=max<=86400; sendDays mask 1..127 = at least one day; valid tz) returning the exact `EmailAccount` update payload. New `updateEmailAccountSettings(_prev, formData)` action (org-scoped `updateMany`, parses `target`-free per-field form data via the pure helpers) + `listEmailAccounts` select expanded with the six pacing fields. `/skrzynki` rebuilt as per-mailbox cards (header keeps email + status + reply-detection badge + `ConfirmButton` Odłącz) with a new `mailbox-settings-form.tsx` (`useActionState`; mostly uncontrolled defaults from the account; native `<input type=time>` -> "HH:MM"; seven `day_0..6` checkbox chips via the `peer` pattern; curated `<Select>` timezone that prepends a saved custom zone so it survives a save; `step=1` number inputs; toast on save). **No migration** (all seven fields already on `EmailAccount`, 1:1 with the engine's `PacingAccount` in `features/sending/schedule.ts`). **Scoped OUT by user choice (config-only):** the dedicated mailbox-health section and the SPF/DKIM/DMARC deliverability-guidance panel — both shown in the mock, both a clean later add (and still listed under "Cross-cutting before launch"). Gates: `tsc` 0, `vitest` 222 (+10), `eslint` clean (`app/(app)` + `features` + `components`), `next build` green. Review: all six correctness checks held (form/action field-name contract across 8 fields, native checkbox-deselect persists off, cleared/NaN/fractional inputs fail closed with no bad write, tz fallback no-dup, org-scoped, no connect/disconnect regression); 1 minor fixed (`step=1`); window `end===1440` confirmed safe (engine windows are half-open `< end`, local minutes 0..1439).

**Sub-project (build order #7) — `dashboards` — DONE + verified 2026-06-09 (code/test/build):** spec `docs/superpowers/specs/2026-06-09-phase-7-dashboards-design.md`, plan `docs/superpowers/plans/2026-06-09-phase-7-dashboards.md`, mocks `docs/superpowers/design/dashboards-mock/` + `dashboards-rate-mock/`. Built brainstorm (two dark-themed makiety — the pulpit, and a side-by-side showing how the rate denominator changes the numbers; user chose **full scope** [pulpit `/` + per-campaign] and the **"contacted"** denominator) -> spec -> plan -> inline TDD + adversarial review. An at-a-glance read of campaign performance, all derived from existing data (no migration, no tracking). Pure `features/metrics/compute.ts` (TDD, +8): `summarizeStatuses`, `contacted` (= total − PENDING − SKIPPED − FAILED), `rate` (null on 0), `computeRates`, `formatPct` (PL comma, "—" on null), `formatInt` (NBSP thousands via `String.fromCharCode(160)`), `buildCampaignRows` (merge per-campaign status rows, order-preserving), `orderFunnel` (reuses `STAGE_ORDER`). `features/metrics/queries.ts` `getOrgMetrics`/`getCampaignMetrics`: org-scoped (CampaignLead has no direct org, so scoped via `campaign:{organizationId}`; `getCampaignMetrics` ownership-checks then returns null for a foreign id), Prisma `groupBy` `_count:{_all:true}` -> `g._count._all`, `Message` OUTBOUND/SENT count for emails. UI: `/` MOVED from a `redirect("/leady")` (`app/page.tsx` deleted) into the `(app)` auth shell as `app/(app)/page.tsx` (server component) + a "Pulpit" nav link (NavLink active only on exact `/`); `dashboard-parts.tsx` (`Kpi`/`FunnelBar`/`MetricStat`, server-rendered, no client JS, no new deps); per-campaign metrics strip on `/kampanie/[id]`. **Rate denominator = contacted** (leads that got >=1 email); open/click rates omitted by design (no tracking model / open-pixel off). Gates: `tsc` 0, `vitest` 230 (+8), `eslint` clean (`app/(app)` + `features` + `components`), `next build` green (`/` now a dynamic route, not a redirect). Review: all 6 correctness checks held (org-scoping airtight on every query incl. the indirect campaign scope; `_count._all` mapping; divide-by-zero -> "—"; clean route move with the (app) auth redirect; `Math.max(1, ...)` funnel guard; valid field reads); 1 minor fixed (NBSP separator so big numbers don't wrap). NOT live-eyeballed (read-only server components; statically verified).

**Sub-project (build order #8) — `shell-and-nav-redesign` — DONE + verified 2026-06-09 (code/test/build):** spec `docs/superpowers/specs/2026-06-09-phase-7-shell-and-nav-redesign-design.md`, plan `docs/superpowers/plans/2026-06-09-phase-7-shell-and-nav-redesign.md`, mocks `docs/superpowers/design/shell-mock/` (3-shell comparison) + `shell-mock-refine/` (active-item finish). Built brainstorm with TWO rounds of visual choice (user picked **variant C** = left icon sidebar collapsing to icons, mobile = collapse-to-icons; then **finish B** = left accent bar for the active item) -> spec -> plan -> inline TDD + adversarial review. Replaces the flat 10-link top bar. Pure `features/nav/active.ts` `isNavActive` (TDD, +2; covers the `/` exact-match edge). ~13 stroke icons in `components/ui/icons.tsx`. `app/(app)/nav-items.tsx` (grouped config: Sprzedaż / Treści / Wysyłka + standalone Ustawienia). Client `app/(app)/sidebar.tsx`: collapse flag via a module-level external store + `useSyncExternalStore` (`getServerSnapshot=() => false` -> no hydration mismatch AND no `react-hooks/set-state-in-effect` lint violation — the reason for not using `useEffect`); responsive collapse in pure Tailwind (`w-14` + conditional `lg:w-[212px]`; labels `hidden` / `hidden lg:inline`) so the rail is icon-only below `lg` and toggle-controlled at `lg+`; left-bar active state (a `before:` accent bar flush to the rail edge + accent icon + fg text); `ThemeToggle` in the footer; account menu via native `<details>` (name/email + org + role badge + `Wyloguj`, `aria-label`ed for icon-only mode). `features/auth/actions.ts` `signOutAction` (the inline `signOut` closure moved here so the client menu can call it). `app/(app)/layout.tsx` refactored from a top `<header>`/`<nav>` to a `[Sidebar][main Container]` flex shell (it now also `findUnique`s the org name; preserves `ToastProvider`/`ConfirmProvider`, the `#main` skip link, `Container`, and the `redirect("/logowanie")` auth guard); `app/(app)/nav-link.tsx` deleted (superseded). No migration. Gates: `tsc` 0, `vitest` 232 (+2), `eslint` clean, `next build` green. Review: all 6 checks passed (hydration via `useSyncExternalStore`, responsive collapse logic, server-action wiring, layout-guard preservation, a11y, no regression); applied 1 important a11y fix (account-menu `aria-label`) + 1 taste fix (active bar flush to the edge); accepted nits (native `<details>` doesn't close on outside-click — fine for an internal tool; `Svg` hardcoded 16px overridden by `size-[18px]`). NOT live-eyeballed by me (the shell is auth-gated, so a headless screenshot hits `/logowanie`) — verified statically + a high-fidelity mock the user approved; the user eyeballs the real shell on :3001.

**Sub-project (build order #9, LAST) — `page-level-visual-pass` — SPECCED 2026-06-09, implementation deferred to a FRESH session** (the user asked to prepare the visual comparison + spec now and implement next session): spec `docs/superpowers/specs/2026-06-09-phase-7-page-level-visual-pass-design.md`, mocks `docs/superpowers/design/polish-mock/` (before/after) + `list-style-mock/` (Tabela vs Karty). Brainstorm: ran the `page-level-visual-audit` workflow (5 parallel auditors -> 131 raw-element sites, 27 enum leaks, 32 copy issues across ~25 surfaces) then two visual mocks for the taste forks. **User choices:** library lists -> **Card rows** (variant K); **full polonization** (translate all enums to Polish labels, `Suppression` -> `Wykluczenia`, `score` -> `ocena`, `research` -> `badania`, drop `(customFields)`/`(honorific)` code leaks; keep `Pipeline`/`follow-up`/`draft`). Shared foundation = `features/enums/labels.ts` (pure label + `Badge`-variant maps for CampaignStatus / CampaignLeadStatus / SequenceCondition / InboundKind / ResearchTypeKind / ResearchRun+BatchStatus / PlaceholderType / Role; `DealStage` reuses the existing `STAGE_LABEL`; export `BadgeVariant` from `badge.tsx`). Genuine DATA tables (`/leady` list, `batch-view` runs) stay the `Table` primitive; everything else adopts Input/Select/Textarea/Field/Note/Switch/Button/Card/EmptyState/Spinner/Badge. Decomposed into 6 slices (foundation FIRST, then leady / kampanie / badania / content [szablony+placeholdery+linie] / sending-pipeline) — disjoint files, so a fan-out workflow or subagent-driven fits. **Resume:** in a fresh session, read the spec, run writing-plans, then implement; pure presentation (do NOT touch server actions/queries or form field `name`s — verify each form still submits the same names). The 8 prior Phase 7 sub-projects are DONE.

### Cross-cutting before launch
- Deliverability: SPF/DKIM/DMARC, conservative warmup ramp, randomized gaps, send windows, suppression honored, open-pixel OFF by default.
- RODO (PL B2B): sender identity + easy opt-out + permanent suppression; data minimization.
- Tracking policy: reply-detection ON, open OFF, click optional (custom subdomain).

## How to resume
1. Read `CLAUDE.md`, `AGENTS.md`, this file. **Next task = EXECUTE the `page-level-visual-pass` spec — it is SPECCED + direction-chosen (2026-06-09); just run writing-plans -> subagent-driven (or a fan-out workflow) from `docs/superpowers/specs/2026-06-09-phase-7-page-level-visual-pass-design.md`.** Direction is locked: Card rows for library lists + full polonization + a shared `features/enums/labels.ts`; 6 disjoint slices (foundation labels module first). Pure presentation — do NOT change server actions/queries or form field `name`s. 8 of 9 Phase 7 sub-projects are DONE (`design-foundation`, `sender-signature`, `dark-ready token migration`, `destructive-action-safety`, `import-customfields-mapping`, `deliverability-settings`, `dashboards`, `shell-and-nav-redesign`); after page-level-visual-pass only the low-priority `team-role-ui` remains (needs an Invitation-model decision; role returned by `requireOrg` but never enforced). **User prefers PL conversation + visual mockups for UX forks (see memory); when a fork is visual, give him rendered comparisons to choose from.** Full Phase 2 (research AI, incl. 2d-4 send-draft) is DONE + verified at code/test/build. Still open before launch: a first live research/scoring/content/draft run (needs a funded `ANTHROPIC_API_KEY` -- see memory) + verify `run-research`, `run-research-batch`, `run-draft`, and the reply cron fire on Inngest Cloud.
2. Specs in `docs/superpowers/specs/`, plans in `docs/superpowers/plans/`.
3. Knowledge graph in `graphify-out/` — `graphify query "<question>"`, `graphify path "<A>" "<B>"`, `graphify explain "<concept>"`; `GRAPH_REPORT.md` for architecture; Obsidian vault for navigation.
4. Migrations: `npx prisma migrate dev`. Tests: `npx vitest run`. Types: `npx tsc --noEmit`. App: the user runs `npm run dev`.

### Local dev gotchas (this machine)
- **Port:** another Next app squats `:3000`, so coldmailing's `npm run dev` may bind `:3001`. But `GOOGLE_MAILBOX_REDIRECT_URI` (and the GCP OAuth client's Authorized redirect URI) are set for **`:3000`**, and mailbox OAuth uses that fixed env redirect (login is host-relative, so it works on any port). Keep coldmailing on `:3000` (free the port first), or align `.env` redirect + the GCP entry + the actual port to one value — otherwise `redirect_uri_mismatch`. `.env` is loaded at dev-server start, so **restart after editing `.env`**.
- **Inngest:** local runs need the Inngest dev server (`npx inngest-cli dev -u http://localhost:3000/api/inngest`, UI at `:8288`). Event functions can be invoked from the panel. **Cron gotcha (seen 2026-06-03):** the dev server can wedge so `scan-mailboxes-for-replies` cron runs stack up in QUEUED and never execute (panel -> Runs shows them QUEUED) -- reply/opt-out scanning silently stops. **Fix: restart `inngest-cli dev`** (resets the in-memory queue; the cron then fires on schedule, verified). To force a scan without waiting for the cron, send the poll event directly: `npx tsx scripts/diag-trigger-poll.ts`. **Confirm the cron actually executes on Inngest Cloud before launch** -- reply/bounce/opt-out detection depends on it.
- **Disconnect cascade:** deleting an `EmailAccount` (the disconnect action) cascade-deletes its `Message` rows and nulls `Campaign.sendingEmailAccountId`. Reply detection needs an OUTBOUND `Message` (a send) from the *currently connected* mailbox.
- **Send window:** if widened for an immediate test, restore Mon-Fri 08-16 (`sendDays=31`, `sendWindowStartMin=480`, `sendWindowEndMin=960`).
